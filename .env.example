# คัดลอกเป็น .env แล้วแก้ค่า — ใช้กับ docker-compose.prod.yml
# สร้าง SECRET_KEY: openssl rand -hex 32
SECRET_KEY=
# สมัคร user แรกเสร็จแล้วเปลี่ยนเป็น false แล้ว up -d ใหม่
REGISTRATION_ENABLED=true
